import { StyleSheet, Text, View } from "react-native";

export const ManageExpense = (props) => {
  return (
    <View>
      <Text>Manage Expense</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
